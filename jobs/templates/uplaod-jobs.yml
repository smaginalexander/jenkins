- job-template:
    name: upload-jobs
    description: Jobs uploader to jenkins
    project-type: pipeline
    parameters:
      - string:
          name: BRANCH
          default: master
          description: Branch for tempest barbican UI tests
    pipeline-scm:
      scm:
        - git-runner
      script-path: jobs/scripts/uploadJobs.groovy